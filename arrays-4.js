let str = ['JavaScript', 'Java', 'C++', 'Python', 'PHP', 'SQL', 'C#', 'C', 'R', 'Kotlin',];

let i = 0;
while (i < str.length){
   if (str[i].length >= 3){
   console.log(str[i]);
   i++
   };
}