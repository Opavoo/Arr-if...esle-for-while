let number = [19, 30, 45, 55, 60, 1, 2, 3, 4, 5];
let accumulatorOdd = number;

for(let i = 0; i < accumulatorOdd.length; i++){
   if(accumulatorOdd[i] % 2 === 0){ 
   console.log(accumulatorOdd[i]);
}
}
